The KNOWN SHORTCUTS list below was resolved from the user's current OmniWM configuration: for each shortcut the user mentioned it gives the EXACT command that shortcut is bound to (or notes it is unbound). When the user's Message mentions one of these shortcuts, name that exact command in the Title and Summary — never substitute a different one — while keeping the user's original shortcut wording. You may state Expected Behavior as that command's normal effect, but do not invent expectations beyond the command's purpose. If a shortcut is listed as unbound, state that it is unassigned. Do not introduce any shortcut the user did not mention.
